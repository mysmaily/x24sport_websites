interface:
  display_name: "Football Mockup Convert (Legacy)"
  short_description: "Routes legacy requests to the master-first skill"
  default_prompt: "Use $football-mockup-convert to route this legacy football reference into the unified $tao-mau-ao-bong-da-tron-goi master-first workflow."
policy:
  allow_implicit_invocation: false
