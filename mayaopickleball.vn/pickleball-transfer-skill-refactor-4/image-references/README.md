# Image references

Place official destination logo files here before running production:

```text
logo.svg
logo.png
```

The pipeline uses `logo.svg` first and falls back to `logo.png` only if SVG rendering is unavailable or fails.

Do not replace these with a generated text badge. Plain text `X24` fallback is forbidden.
